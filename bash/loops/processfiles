#!/bin/bash

# Process files, skip hidden files and all folders

for file in .* *; do
  [[ "$file" == .* ]] && echo "Skipping hidden file: $file" && continue
  [[ -f "$file" ]] || continue
  echo "Processing: $file"
done
